import React from "react";
import "./style.css";
export const DateSlideshow = ({ date }) => {
  const handleTakeHour = () => {};
  return <button onClick={handleTakeHour}>{date.dateShow}</button>;
};
